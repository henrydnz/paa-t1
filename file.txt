Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.Este e um arquivo de texto para testar o algoritmo de compressao de Huffman.
O algoritmo de Huffman cria uma arvore binaria baseada na frequencia das palavras.

Palavras frequentes como o, a, de, e, um, uma terao codigos binarios menores.
Isso garante que a compressao seja eficiente e o arquivo fique menor.

Aqui temos uma quebra de linha.
E aqui temos outra quebra de linha.
    
    Temos tambem espacos extras e tabulacoes nesta linha.

Fim do teste de compressao.
Fim do teste de descompressao.
Tudo deve sair exatamente igual ao original.